@{ ViewData["Title"]="Login"; }<div class="form-page narrow"><h1>Customer Login</h1>@if(!ViewData.ModelState.IsValid){<div class="alert error">@Html.ValidationSummary()</div>}<form method="post" class="formcard">@Html.AntiForgeryToken()<label>Email<input type="email" name="email" required /></label><label>Password<input type="password" name="password" required /></label><button class="btn full" type="submit">Sign In</button></form><p class="muted center"> </p></div>
