namespace MotshwaneConsortiumGroup.Models;

/// <summary>Placeholder until Thato's Staff/User entity replaces it — same shape expected: Id, Name, Email.</summary>
public class Staff
{
    public int Id { get; set; }
    public string Name { get; set; } = "";
    public string Email { get; set; } = "";
}
